=== Linux-PAM/libpam/pam_handlers.c
==================================================================
--- Linux-PAM/libpam/pam_handlers.c  (revision 250)
+++ Linux-PAM/libpam/pam_handlers.c  (local)
@@ -264,6 +264,7 @@
 	    }
 	}
 end:
+	continue;
     }
 
     return ( (x < 0) ? PAM_ABORT:PAM_SUCCESS );
