Index: Linux-PAM/libpam/pam_handlers.c
===================================================================
--- Linux-PAM/libpam/pam_handlers.c	(revision 297)
+++ Linux-PAM/libpam/pam_handlers.c	(working copy)
@@ -314,6 +314,7 @@
 	    }
 	}
 end:
+	continue;
     }
 
     return ( (x < 0) ? PAM_ABORT:PAM_SUCCESS );
